# django_practice

A simple Django practice project for my lab work (Django + Git).

## What’s inside
- Django project setup
- `apps/` folder for lab modules
- Basic Git workflow (.gitignore, commits, push)

## How to run
1) Create/activate a virtual environment  
2) Install dependencies:
   - `pip install django`
3) Run migrations:
   - `python manage.py migrate`
4) Start server:
   - `python manage.py runserver`

## Notes
This repository is for learning and practice.
